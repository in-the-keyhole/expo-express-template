{
    "name": "root",
    "private": true,
    "engines": {
        "node": "22.12.0",
        "yarn": "1.22.x"
    },
    "dependencies": {
        "root": "file:"
    },
    "scripts": {
        "build": "cd client && npm install -g yarn && yarn build && cd ../server && yarn build && cd ..",
        "buildwin": "cd client && npm install -g yarn && yarn buildwin && cd ..\\server && yarn buildwin && cd ..",
        "start": "cd server && yarn start",
        "startwin": "cd server && yarn startwin"
    }
}
